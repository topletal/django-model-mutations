language: python

python:
  - 3.5
  - 3.6
  - 3.7
  - 3.8

before_install:
  - python --version

install:
  - pip install pipenv
  - pipenv install --dev

script: pytest

after_success:
  - codecov # submit coverage
